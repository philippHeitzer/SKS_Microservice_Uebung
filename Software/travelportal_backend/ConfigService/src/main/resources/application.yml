server:
  port: 8888

spring:
  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: classpath:configs
        encrypt:
          enabled: false

encrypt:
  key: topsecret

